function [z_p] = modeloSimplificado_accZ(gama)
    global plane envirmnt
        % gama, psi, vel, mass
    % envirmnt
        % g, p, S, C_d, v_fluid
    z_p = -plane(3)*sin( plane(1) );

end
       