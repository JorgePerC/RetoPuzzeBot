function [y_p] = modeloSimplificado_accY(psi)
    global plane envirmnt
        % gama, psi, vel, mass
    % envirmnt
        % g, p, S, C_d, v_fluid
    y_p = plane[3]*cos(plane[1])*sin(plane[2]);

end
       